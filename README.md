# nodeapi-graphql-typescript
A skeletal GraphQL API written in Typescript

#### Steps
```
$ yarn
$ yarn start
```

There is also a 'watch' task will watch for changes in `src/` and reload the running node server:

```
$ yarn watch
```
